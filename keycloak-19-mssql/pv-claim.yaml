apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ashis-claims1
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 0.5Gi
  volumeName: ashis-tasks2
